# 双入口滤油与泄压总成：工程师自然语言输入示例

两个外部压力入口进入一个不可现场拆分的总成。每个入口设置一个压力开关，用于测量对应单向阀入口侧压力；每个入口经过对应的单向阀后汇合。汇合油路经滤器到压力出口；滤器出口设溢流阀，溢流阀回油至回油接口。

已知约束：

- 压力开关只取压，不承载主油流。
- 单向阀方向为入口至汇合油路，两个单向阀用于隔离入口之间的反向串流。
- 上述六个成员均属于同一不可现场拆分装配。
- 未提供压力等级、流量、阀设定值和工作模式；生成结果必须保持概念阶段并提示补充。
