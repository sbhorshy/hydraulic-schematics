# 现状基线（before）：1# 系统 v2.3 手工管线

> 对应票：现状基线度量：1# v2.2 图 B1–B7 实测与人工迭代存档（GitHub #11），父图：复杂拓扑端到端工具化地图（#10）。
> 用途：到站验收「新机制度量不劣于现图」的阈值基线；布局选型原型（#17）的对比锚点。
> 测量日期：2026-08-31。测量对象：`1#系统.layout.json` @ revision 2.3 渲染的 `1#系统原理图.svg`（extern 用户边界写法）。

## 1. 构图度量实测（validate_sheet.py 构图预算面板，concept 档 v1）

| 项 | 实测 | 预算 | 状态 |
|---|---|---|---|
| B1 线线交叉 | 0 | =0 | pass |
| B2 折返次数 | 全图 24，单线最大 3 | ≤40 / ≤3 | pass |
| B3 绕行比 | max **2.373** | ≤1.5（边界 4.0） | **超限 WARN** |
| B4 最短走线段 | 8.0 px | ≥8 | pass（压线） |
| B5 节点盒最小净距 | 40.0 px | ≥40 | pass（压线） |
| B6 容器走廊 | group_padding 20 | ≥14 / 避让走廊 ≥12 | pass（avoid_corridor 未测，v1 裁剪） |
| B7 标签净空 | 未测 | — | v1 裁剪 |

注：B4/B5 双双压在预算下限，说明手工布局是在阈值约束下"贴边"收敛的；B3 的 2.373 超限线为油箱回油顶绕支路，属**存量超限**（当前以 WARN 放行，未豁免登记）。

## 2. 校核现状

- preflight（输入预检）：`cleared_for_layout`，0 findings。
- 几何校核：**passed（fail 0 / warn 6）**：
  - V13 共线重叠 ×2（均汇于同端口，按连通处理）；
  - V4 三通点 (1029,1258) 不在任何母线 x 上；
  - V5 悬空端口 9 个（须图签计数标红）；
  - V9 非 annotated 符号 14 个（provisional/draft/none——concept 成熟度允许）；
  - V19/B3 绕行比 2.373 超限。

## 3. 效率基线（人工迭代成本，from sess_4ea30791 + revision_log）

**总耗时约 2 小时**（完整绘图 session，用户口径），产出当前 14 受控件 + 2 边界 + 4 母线 + 2 taps 规模的图。

### 3.1 布局返工轮次（layout.json revision_log，初版后 4 轮全手工坐标）

| 轮次 | 修改内容 | 触发原因 |
|---|---|---|
| 2.0 重排 | QDP 移位、CDF 移顶部 rot270、气侧组件上移、PRV-001/EDP/FSOV 行调整、吸油两行换序、新增 lane 245 与 vlanes 290/20 | 初版构图冲突（交叉/走廊） |
| 2.1 修 | @CASE 移 620 让开吸油走廊；QDP 改接 @MANIFOLD；PF 右移消折返；CDF 上移；顶绕走廊改 100 | 走廊冲突 + 接头折返 |
| 2.2 修 | QDP 上移；@MANIFOLD 母线底回调 593.8；RF→@RET 支路不再跨母线 | 支路跨母线 |
| 2.3 修 | PF 移 (1000,254) 让 @RET 顶绕竖线 940；CDF 宽收 60 消进线短桩穿盒 | 线穿节点盒（V2）+ 顶绕冲突 |

模式归纳：每轮都是「人肉解一个多变量空间布局谜题」——动一个元件牵连母线底、走廊值、邻行 y；四轮中三轮由**走廊/跨线冲突**触发，一轮由**穿盒**触发。

### 3.2 AI 管线结构性成本（session 证据）

- **上下文耗尽续接**：会话中途断续续接（2026-08-31T01:11:40Z 续接点），需重新定向读规范。
- **需求变更（规格漂移）**：01:13:41Z 用户要求用户框改 `hydraulic_user` 名框——AI 花 3 次工具调用核验规范/渲染器/示例支持；该变更最终未落本工作目录图（仍是 extern 写法），悬为待办。
- **全手工坐标**：layout note 自述"全部坐标手工给定，无自动布局"——AI 逐轮手填/微调数字。

### 3.3 人工介入点清单

1. 需求变更 1 次（用户框规范更新，01:13Z）。
2. 概念级架构判断：FSOV 装吸油侧、双泵并联无隔离、蓄压器置于压力滤下游等（沿 system-1 审查卡），及新增件安装位置判断。
3. 感知回读：V16 需人工保障 `sheet-readback.png` 像素探测条件。

## 4. 复测口径（after 怎么比）

端到端全自动重跑后，用**同三把尺**对比：

1. 构图度量：同面板复跑，要求 B1–B7 全 pass 且 B3 不劣于 2.373（目标消除该存量超限）、B4/B5 不靠压线凑数；
2. 校核状态：preflight 0 findings + 几何校核 fail 0；WARN 数不高于 6（V9 符号成熟度类 WARN 随 catalog 演进单独披露，不计入劣化）；
3. 效率：返工轮数（revision_log 增量）与人工介入点数，对照本文 3.1/3.3。

## 5. 测量环境备注

- validate_sheet.py 从技能 scripts 复制到本目录、三处路径常量改指本目录文件后运行（SKILL.md 纪律）；产物 `validation-report.json`。
- 基线测量未改动任何图面/意图文件，纯只读测量。
