# 到站演练报告：1# 端到端全自动重跑（2026-08-31）

> 对应票：#10（wayfinder 地图，目的地判定）、#11（三把尺口径）、#12/#13/#14（链路各环）。
> 结论先行：**三把尺全过，宣告到站。**

## 1. 演练定义

从工程师受控清单模板出发，全链零手工坐标、零人工介入：

```
1#清单受控模板.yaml ──(模板门禁: 三向对账+签认分级)──> 1#系统.intent.yaml
  --layout_engine(规则 R1–R14 + kiwi 守门 + 寻优阶段3)--> layout
  --proto_render(渲染+结构自检)--> SVG
  --Inkscape 回读重出(1:1)--> validate_sheet(fail 0 判绿)
  ↑ validate_driver 有界轮次驱动，收敛失败才带结构化报告退出
```

命令：`python validate_driver.py --workdir driver-run-arrival --optimize --rounds 2`
结果：**轮 1 收敛 fail 0，全程 77.7s**，无处方、无残差。

## 2. 三把尺判定（口径：现状基线-v2.3.md §4）

### 尺 1 构图度量——B1–B7 全 pass ✓

| 项 | 基线（手工 v2.3） | 本次全自动 | 判定 |
|---|---|---|---|
| B1 交叉 | 0 | 0 | 持平 |
| B2 折返 | 24 / 单 3 | 23 / 单 3 | 略优 |
| B3 绕行比 | **2.373 超限 WARN（存量）** | **1.447 pass** | **存量超限消除** |
| B4 最短段 | 8.0（压线） | 16.0 | **离压线，裕量翻倍** |
| B5 盒净距 | 40.0（压线） | 43.0 | **离压线** |
| B6 容器走廊 | 20 | 20 | 持平（avoid_corridor 未测口径同基线） |
| B7 标签净空 | 未测 | 未测 | v1 裁剪口径同基线 |

### 尺 2 校核状态——fail 0、WARN 5 ≤ 6 ✓

- preflight：`cleared_for_layout`，**ERROR 0**；findings 1 条 = `W-SIGN-UNSIGNED`
  （concept 档未签认的分级披露，#12 定案的新门禁预期行为，按 V9 同款
  「随机制演进单独披露不计入劣化」处理；draft 及以上 maturity 会被硬拦）。
- 几何校核：**passed（fail 0 / warn 5）**。基线 6 条中的 V19/B3 超限 WARN
  已消除；余 5 条 = V13×2（端口汇融豁免）+ V4 + V5 + V9。
- V9 披露：非 annotated 符号 22 个（基线 14 个）——差额为 8 只
  hydraulic_user provisional 名框（catalog 演进），按尺注单独披露。

### 尺 3 效率——0 返工轮、0 人工介入点 ✓

| 对照 | 基线（§3） | 本次 |
|---|---|---|
| 布局返工轮 | 4 轮全手工坐标 | **0**（1 轮收敛） |
| 人工介入点 | 3（需求变更/架构判断/回读保障） | **0**（架构判断前置在 intent+审查卡；回读已固化为驱动器卫生不变量） |
| 墙钟 | ~2 小时 | **77.7 秒** |

### 拓扑追溯一致（地图对标项）✓

22 实例 ↔ 清单 18 行逐项映射（追溯清单节点表带清单项号）；连接 39 条
（液压 37 + 气侧 taps 2）；模板↔intent 三向对账干净（双向背书）。

## 3. 披露项

1. 确认单未签认（concept 档 WARN 放行；正式出图前须逐行签认翻 signed）。
2. B6.avoid_corridor 与 B7 未自动测量——v1 裁剪口径与基线一致，非本次退化。
3. 引擎 `--ref` 仅承载 labels 呈现文案，坐标零回流（引擎注释明示）。
4. 坐标契约显式化（drawable 矩形+EDGE_MARGIN，删 canvas_shift_x）为到站后
   沉淀批工作（#14 定案），本次以 SHIFT=30 现行口径跑通。

## 4. 产物与复现

- `driver-runs/arrival/`：convergence-report.json、validation-report.json、
  原理图.svg（首张全自动出图）、追溯清单 md。
- 复现：`cd proto/frozen && python validate_driver.py --workdir <dir> --optimize --rounds 2`
- 链路工具：preflight.py（模板门禁）/ layout_engine.py（三阶段）/
  proto_render.py / validate_sheet.py / validate_driver.py（驱动+处方表）/
  topology_confirm.py（确认单+对账）。

## 5. 到站后尾款（不在本图范围，按 #14 定案排序）

1. 沉淀批：技能链修通（TANK-001.suction_out 崩溃）+ 0.3-draft 类型回登记
   + wire_taps 上游吸收 + 坐标契约显式化（同步 sync_snapshot 策略）。
2. 确认单签认流：正式出图前的逐行签认实践。
